services:
  backend:
    build: .
    container_name: afia-backend
    ports:
      - 8000:8000
    env_file:
      - .env
    environment:
      POSTGRES_HOST: db
      POSTGRES_PORT: 5432
      DATABASE_URL: postgres://postgres:postgres@db:5432/afia
    networks:
      - backend-network
    depends_on:
      - db

  db:
    image: postgres:15-alpine
    container_name: afia-db
    ports:
      - 5432:5432
    env_file:
      - .env
    volumes:
      - db-data:/var/lib/postgresql/data
    networks:
      - backend-network

volumes:
  db-data:

networks:
  backend-network:
    driver: bridge
